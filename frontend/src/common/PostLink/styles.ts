import styled from "styled-components";

export const PostLinkW = styled("a")`
  color: var(--main-color-1);
  .is-post-title {
    color: var(--main-color-1);
    font-size: 2em;
    font-weight: 500;
  }
`;
export const Heading = styled("h2")`
  z-index: 10;
  font-family: var(--serif-font);
  text-transform: uppercase;
  margin-top: 0;
  margin-bottom: 0;
  font-size: 5em;
  font-weight: 200;
  line-height: 1.1em;
  position: relative;
`;
